name: todo_app
description: Simple TODO App using Flutter and Supabase
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Supabase (USED in app)
  supabase_flutter: ^2.8.0

  # Icons (USED automatically by Flutter)
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
